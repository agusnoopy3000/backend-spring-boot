package cl.huertohogar.huertohogar_api.model;

/**
 * Roles que puede tener un usuario en el sistema.
 */
public enum Role {
    /**
     * Rol para usuarios estándar (clientes de la app móvil/web).
     */
    USER,
    /**
     * Rol para administradores del sistema.
     */
    ADMIN
}
